import type { Selections } from "./types.js"

export const DEFAULT_SELECTIONS: Selections = {
  projectName: "my-app",
  fe: "nextjs",
  be: "convex",
  db: "convex",
  auth: "clerk",
  ui: "none",
  email: "resend",
  pm: "pnpm",
}
