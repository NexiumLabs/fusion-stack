import type { Selections } from "./types.js"

export const DEFAULT_SELECTIONS: Selections = {
  projectName: "my-app",
  fe: "nextjs",
  be: "convex",
  db: "convex",
  orm: "none",
  dbProvider: "none",
  auth: "clerk",
  ui: "none",
  email: "resend",
  addons: [],
  skills: [],
  pm: "pnpm",
  apiLayer: "none",
  srcDir: true,
}
